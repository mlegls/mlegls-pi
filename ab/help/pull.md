ab pull ING-ID...

Tool results over a few hundred bytes are read with attention to the conversation:
peripheral parts are skimmed or omitted and marked with an ing-… id. pull prints the
original text. For exact output from the start, pass raw: true to the bash tool.
