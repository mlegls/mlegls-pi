ab skill PATH

Load a SKILL.md (or its directory), expanding its dynamic shell placeholders once,
with PI_SKILL_DIR and PI_WORKSPACE set. Relative references in the skill resolve
from PI_SKILL_DIR; workspace commands run in PI_WORKSPACE.
