# Pull request

remote trackers only; the vault writes the same body into the ticket's result. use the repo's template when there is one, else `pr`, ending with:

```markdown
Closes #<issue this completes>
Part of #<unfinished parent>
```
