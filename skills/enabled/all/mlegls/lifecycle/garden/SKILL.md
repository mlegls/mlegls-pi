---
name: garden
description: "Use to garden a repository scope: code, tests, docs, issues or skills."
argument-hint: "a scope or observed strangeness"
---

Make the repository worth copying, with less to understand and claims that agree with actual use.

1. Bound the pass: starting revision, included surfaces and questions. Reuse existing findings, revalidating them against the current subject.
2. Inspect real specimens for strangeness. Start from the underlying problem: would we choose this implementation today, knowing what the framework and libraries provide? Articulate what makes an instance bad and a nearby legitimate instance fine.
3. Encode that distinction as a deterministic lint where syntax suffices, or a narrow Jev judgment where meaning matters (`typesafe-ai`). Calibrate against the specimens before sweeping. Supply the context the judgment needs; file boundaries aren't evidence boundaries.
4. Sweep the declared scope. Review candidates and false positives, refining the check. Jev flags candidates; the reviewing agent establishes the finding and repair.
5. Repair agreed cases with `implement`; verify changed behavior through `verify-story`. Keep discovery/auditing distinct from repair execution. Group coherent repair families into tickets rather than making an issue per flag. Return consequential choices to shaping; ready siblings can proceed.
6. Retain checks that earn their upkeep beside repository tooling. Start semantic checks as advisory; promote to gates on evidence of useful detection and tolerable false positives. Record coverage, exclusions and unresolved findings with the revision and checks used. Finish when confirmed findings are repaired or deliberately moved into separate work.

Keep raw sweep results in an attachment, decisions and executable work in issues (`tracker`). Use `orchestrate` for a pass larger than one session. A pass leaves better precedent and reusable detectors; zero findings and more rules are not goals.
