---
name: git-worktree
description: Do work in a temporary git worktree to keep the main working tree clean.
---

# Git Worktree

Create a temporary worktree off HEAD, do the requested work there, commit, merge back, clean up the worktree and branch.
