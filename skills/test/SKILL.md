---
name: test
description: Write and run tests for recent work, following existing test conventions.
---

# Test

Find existing tests to learn location, framework, style, and helpers. Write and run tests for recently changed code (`git diff` to identify scope unless user specifies). Fix failures until green.
