---
name: make-skill
description: Create a new pi skill in this repo's skills/ directory.
---

# Make Skill

Create `skills/<name>/SKILL.md` with frontmatter (`name`, `description`) and a concise body. Skills here are slash-command shortcuts — the description triggers loading, the body gives just enough context for a frontier model to do the right thing. See sibling skill directories for reference.
