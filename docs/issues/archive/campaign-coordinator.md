---
stage: done
part-of: "[[projects/mlegls-pi/issues/agentic-setup-reorg]]"
---

fable as campaign coordinator: maintains a gantt (critical path method — keep the critical path busy, pack slack with cheap independent work), each bar annotated with the pool it consumes so the chart doubles as the budget view; the interactive interface for state and decisions. split idea→goal (fable) from goal→tickets (astra), with a jev check that each ticket falls within the goal as written, since astra will go along with its own framing.

grill: what the coordinator's own session may do (the audit shows coordinators hack in place, 1218 edits after first spawn, on the anthropic pool), and whether the gantt lives in the tracker ("[[projects/mlegls-pi/issues/archive/operon-adapter]]") or in the board.

decisions:
- 2026-09-20: dissolved across three project notes in the vault ([[control plane]], [[routing]], [[mlegls-pi]]) after [[a return to cyborg augmentation]]. no coordinator session: the interface is the project doc, and agents act on blocks through four commands separated by what they may write (comment, propose, session, implement), on command not on save. gantt/kanban are views over issue frontmatter in the vault ([[projects/mlegls-pi/issues/tracker-obsidian-plugin]]), not the board. fable idea→goal, astra goal→tickets, jev containment check live in [[routing]] as the keymap's soft part. the coordinator-hacks-in-place finding is answered structurally: execution only through implement from a ticket.
