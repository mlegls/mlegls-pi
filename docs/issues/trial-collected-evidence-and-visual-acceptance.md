---
stage: ticket
assignee: agent
author: session:01a0de2d-a2d2-7063-8b18-72c4b0fc2199
---

Try [[projects/mlegls-pi/verification-evidence]] on three real deliveries: two rendered journeys and one nonvisual API/CLI/library journey. Coordinate with their existing supervisors rather than claiming or dispatching duplicate work. Keep the current collector model preference; visual judgment uses the visual-reviewer operating point.

Before the trial, coordinate a shared ab daemon restart with active owners so the new loop code is loaded. Old carried workers need the new evidence handoff; do not silently accept an old `stories` map. At implementation time the active Concept owners were `mail/168492b9` and `mail/09f91334`; rediscover current owners rather than treating those addresses as permanent.

Done when the three packets survive integration and worktree retirement, rendered packets have an actual stronger-model image judgment, and the report records model/effort, setup failures, parent interventions, missing evidence requested and defects found. Distinguish Jev navigation from deterministic Playwright replay and from strong-model judgment. Report accepted completion cost where available, not just collector tokens. Recommend whether the split or collector operating point should change; no routing-default change is required by the trial.
Count avoidable handoffs and duplicated work as well: a reviewer repairing and re-driving directly is a success, not a violation of separation of duties. Identify stages that could be collapsed without losing a useful observation or judgment.

The implementation's disposable Git/worktree smoke covered pipeline wiring, refusal of empty outcomes, missing-state rejection and direct reviewer repair with updated evidence. Its image was a fixture and worker transport was mocked: it establishes neither live image-review quality nor Jev navigation reliability.
