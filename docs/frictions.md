# Frictions

- `exec` moves operations below pi's tool-event boundary. Inner `read`/`sh` calls do not fire individual tool events, but outer tool-result middleware can still rewrite their displayed output. During dogfooding, pi-better-skills interpreted an anchored SKILL.md shell block as a command containing line numbers and anchors and reported `command not found`. The source snapshot remains raw; the displayed text is not guaranteed to be. An explicit skill-loading capability or exec-aware middleware would separate raw source from executable skill expansion. Next: simplify.
- `exec` edits cross JSON, TypeScript template-literal, and source-language escaping layers. A replacement containing a literal `\n` became an actual newline because the edit tag consumes cooked strings; embedded backticks and `${...}` also need protection. Raw edit literals or a clearer construction idiom would reduce this cognitive load. Next: simplify.
- `show(await sh(...))` displays the shell result as an inspected object: multiline stdout becomes quoted strings with escaped newlines. Showing `.stdout` is readable but drops exit status and stderr unless selected separately. A compact shell-result renderer could retain both readable output and failure metadata without changing the structured value. Next: simplify.
